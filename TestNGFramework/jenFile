node("myAgent") {                           // Top level agent
    timeout(unit: 'SECONDS', time: 5) {     // Options directive
        stage("One"){
            sleep 10
            echo 'hello'
        }
    }
}